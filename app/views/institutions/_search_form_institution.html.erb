<div id="institution_search_form">
  <%= form_tag search_action_url,
               :method => :get,
               :class => 'search-query-form form-inline institution-search' do %>

      <%= render_hash_as_hidden_fields(params_for_search(:f => {'institution_name_ssim' => [document[blacklight_config.index.title_field.to_sym]]}).except('q', 'search_field', 'qt', 'page')) %>

      <div class="input-append pull-left">
        <label for="q" class="sr-only"><%= t('blacklight.search.form.institution') %></label>
        <%= text_field_tag :q,
                           params[:q],
                           :placeholder => t('blacklight.search.form.institution'),
                           :class => 'search_q q input-xlarge',
                           :id => 'q',
                           :autofocus => true %>
        <button type="submit" class="btn btn-primary search-btn" id="institution-search">
          <span class="submit-search-text"><%=t('blacklight.search.form.submit')%></span>
          <span class="glyphicon glyphicon-search glyphicon-white"></span>
        </button>
      </div>
  <% end %>
</div>