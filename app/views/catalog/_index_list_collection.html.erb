<%# index list view template  %>
<div class="collection_index_container">
  <%# unless customization needed, use generic item index view %>
  <%= render :partial => 'catalog/_index_partials/index_list_default_content',
             :locals => {:document => document, :document_counter => document_counter} %>
</div>