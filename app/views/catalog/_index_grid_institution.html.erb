<%# index grid view template  %>
<div class="institution_index_container">
  <div class="item_index_thumb">
    <% thumb = image_tag('dc_library-icon.png',
                         :alt => document[blacklight_config.index.title_field.to_sym],
                         :class => 'index_thumb') %>
    <%= link_to thumb, '/institutions/' + document[:id] %>
  </div>
  <div class="item_index_title">
    <%# link_to_document(document, :label => :label_ssim, :counter => (document_counter + 1 + @response.params[:start].to_i)) %>
    <%# link_to_facet(document[:label_ssim].first, 'collection_name_ssim') %>
    <%= link_to document[blacklight_config.index.title_field.to_sym], '/institutions/' + document[:id] %>
  </div>
</div>