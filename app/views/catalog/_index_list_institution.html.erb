<div class="institution_index_container">

  <div class="media">
    <% thumb = image_tag('institution_index.png',
                         :alt => document[blacklight_config.index.show_link.to_sym],
                         :class => 'index_thumb media-object') %>
    <%= link_to(thumb, '/search/' + document[:id], :class => 'pull-left') %>

    <div class="media-body">

      <div class="item_index_title">
        <%= link_to(document[blacklight_config.index.show_link.to_sym],
                    '/search/' + document[:id]) %>
      </div>

    </div>

  </div>

</div>